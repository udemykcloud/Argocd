---
apiVersion: v1
kind: Service
metadata:
  name: moderator
spec:
  selector:
    app: moderator
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP   # ✅ this line is correctly indented

---
apiVersion: v1
kind: Service
metadata:
  name: moderator-canary
spec:
  selector:
    app: moderator
  ports:
    - port: 8080
      targetPort: 8080   # ❌ if there's a stray space or tab here, YAML breaks
